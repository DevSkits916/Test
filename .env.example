# Copy this file to .env and adjust the values for your deployment.
# Empty values are left blank on purpose so you can fill them in when needed.

PORT=3000
BIND=0.0.0.0
POLL_INTERVAL_SECONDS=60
USER_AGENT=SoraInviteCodeHunter/1.0 (+https://render.com/docs/deploy-flask)
STORE=memory
SQLITE_PATH=data/codes.db
DISCORD_WEBHOOK_URL=
ADAPTERS=reddit_search,reddit_subs,generic_rss,generic_html
CONFIG_PATH=config/sources.json
MIN_LEN=5
MAX_LEN=8
